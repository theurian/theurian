# gitleaks false-positive suppressions, one exact fingerprint per line
# (`<commit>:<path>:<rule>:<line>`, as gitleaks prints it). Fingerprint-scoped on
# purpose: the `generic-api-key` rule stays at full strength everywhere else, and
# a suppression names the one line it forgives rather than the rule.
#
# `_AUTH_POLICY_SHA` is the SHA-256 digest of an auth-policy body file, reused in
# the duplicate-contentFile refusal test to point two revisions at the same file.
# It is a content digest, not a credential; gitleaks' generic-api-key rule trips
# on its 64-hex entropy.
59a50ca72320ae0477f709f9fa67d2454a364a7e:packages/theurian-core/tests/integration/test_setup_migrations_checker.py:generic-api-key:149
#
# TEMPORARY (PR #626 unblock): a synthetic GraphQL node-id fixture (`thread_key`)
# on the concurrent slice-3 branch — a false positive the PR-gate scan reaches
# only because `fetch-depth: 0` fetches every branch and gitleaks scans all of
# it (the cross-lane hazard is filed as its own issue). The slice-3 lane owns
# the permanent fix — renaming the fixture to a low-entropy value — at its next
# branch touch, at which point this line is removed.
e5dc6ef078a5b888c36b1afaa844d7f966779f28:packages/theurian-core/tests/integration/test_review_search_builder.py:generic-api-key:243
