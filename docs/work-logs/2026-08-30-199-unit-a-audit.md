# 2026-08-30 — #199 unit A: auditing the threat model's claims against `src/`

Anchor: `main 06de58a`, tree clean. Every count below is the output of a command,
re-run against that commit while writing this log; the commands are listed at the
end. The corrections themselves are in
[PR #425](https://github.com/theurian/theurian/pull/425).

#199 exists because three separate defects turned out to be one shape: a
threat-model sentence describing a control that `src/` does not implement, or
implements differently. #129, T-15 and T-6 each arrived as its own bug. This unit
audited the whole document rather than the next symptom.

## What the audit is a population of

An audit is only as good as its key, and a key is a claim: *these are all the
places a control claim can hide.* The key went wrong twice before it held, and
both regressions have the same cause, which is the finding this log exists to
record.

### The three keys, v3

| Key | What it selects | Members at `06de58a` |
| :-- | :-- | --: |
| **Container census** | every bold-labelled block opener, at any blockquote nesting, classified by whether its body names a `src` symbol | 276 openers · 88 already inside another key · 188 outside · 15 needing triage |
| **1e retraction blocks** | structural: a marker is a member iff it *opens* a retraction block not already inside one | 21 members (16 corrections + 5 discharges), 7 continuations |
| **Verb sweep** | active-voice control verbs anywhere in the document, each hit assigned to its owning block | 134 hits · 32 inside a keyed member · 102 triaged outside |

The census is load-bearing and the verb sweep is the cross-check — which is the
opposite of how the residual was framed going in. `:4940`, labelled **"Control,
part A — read-side, serve-time"**, carries **zero** verb-sweep hits: its claim is
spelled "reads each … referenced id", and no listed verb appears. The census
finds it; the sweep does not. Two keys covering each other's blind spots is the
design, not redundancy.

### Key history, and why each version regressed

| Version | Key for control claims | What it missed, and why |
| :-- | :-- | :-- |
| **v1** | `line.startswith("**Controls")` | A prefix match on one plural noun. Missed the three `*Future controls, not shipped:*` paragraphs and every `> **Corrected …` blockquote — 2 of the 4 corrections found live outside all of v1's keys. |
| **v2** | v1 + a `1d` key for the Future-controls paragraphs + a regex `1e` key for retraction markers | The regex could not settle 1e: `"What it said."` is a *continuation* at `:1717` and a block *opener* at `:4258` — the same string, two roles. v2's first count of 17 over-counted by 3 continuations and under-counted by 7 top-level markers. |
| **v3** | container census (openers, any nesting, classified) + structural 1e + verb sweep as cross-check | See the closure statement below. |

**Each version stopped at a boundary its own author expected.** v1's author
expected a control claim to be labelled `**Controls`, so two of them labelled
`**Control` — singular — were invisible, missed by one character. v2's author
expected a marker's text to determine its role, so a string that is an opener in
one place and a continuation in another could not be counted. Neither key was
careless; each encoded an assumption its author did not know was an assumption.
That is why a stated key matters more than a careful reader: a reader cannot
attack an assumption nobody wrote down.

### Totals

| Key | Members |
| :-- | --: |
| POP-1a Controls blocks | 23 |
| POP-1b control-claim table rows (9 tables) | 62 |
| POP-1c floating prose assertions | 11 |
| POP-1d Future-controls paragraphs | 3 |
| POP-1e retraction/discharge blocks | 21 |
| K-2 table rows the 1b key missed (`:3786` = 3, `:5092` = 4) | 7 |
| **keyed total** | **127** |
| V3-1: control blocks the `Controls` prefix key never reached | +5 |
| **total dispositioned** | **132** |

Dispositions across the 127: **(a) verified against `src/` 118 · (b) not shipped
with an OPEN owner 1 · recorded-unfiled 3 · (c) correction needed 5.**
118 + 1 + 3 + 5 = 127. V3-1's five members were each read against `src/` and are
**all true** — a coverage gap, not a correction — which is why they are counted
apart from the 127 rather than folded in.

## The closure statement

**v3 is the terminal *achievable* form, not a proof.** A claim that is neither a
bold-labelled block opener nor verb-bearing escapes both keys. That escape space
is characterized rather than assumed away, and at `06de58a` it is **measured
empty**: the two keys' union covered everything the census surfaced, so any
future finding there is a named v4, not an open-ended regress. What that buys is
a bounded argument — the next reader attacks the escape space or the measurement,
both of which are written down, instead of re-deriving the population.

One known false-positive mode of the structural 1e key, found while writing this
log: a bold run that begins a line by accident of *wrapping* is indistinguishable
from a block opener. A sentence in this PR's own T-16 correction wrapped so that
`**closed**` started a line, and the key counted a 22nd discharge block that does
not exist. Rewrapped rather than special-cased — the key is right that a
line-opening bold run is how this document opens a block, and prose that wraps
that way is ambiguous to a human reader too.

## The meta-finding

**Four enumeration misses in this arc share one root cause: a key chosen by what
its author expected to find.**

1. v1's `**Controls` prefix missed `**Control, part A/B`, by one character.
2. v1's key reached no `*Future controls, not shipped:*` paragraph, where C-1 lives.
3. v1's key reached no `> **Corrected …` blockquote, where C-3 lives — the
   highest-risk text in the document, since a correction block is written to
   replace a falsified premise and then never re-derived.
4. The verb sweep, proposed as the primary key, would have missed `:4940` outright.

The defense is not a more careful reader. It is **a mechanical key run to
exhaustion, plus a stated key that a second reader can attack.** The
census-plus-sweep pair is that defense by construction: the census is mechanical
and total over containers, the sweep is orthogonal to it, and both are written
down here in a form that can be re-run and disagreed with.

The corollary the corrections themselves demonstrate: **a premise that has to
stay true belongs in a test that reddens, not in a sentence.** C-1's conclusion
survives because
`test_the_shipped_modules_that_name_a_watched_config_key_are_the_recorded_ones`
pins the reader set as an equality over the whole source tree. C-3's premise had
no such pin, which is why a correction block written to fix a wrongly-named
mechanism went stale the same way within one milestone.

## The corrections

Four claims were false. Each is narrowed to the fact that is true; no risk entry
was deleted and no true control description was weakened.

| ID | Site | The false claim | Measured |
| :-- | :-- | :-- | :-- |
| **C-1** (HIGH) | `threat-model.md` T-7 Future-controls; `infrastructure/github/__init__.py`; `docs/architecture/review-knowledge.md` | "No reader of `.theurian/config.yaml` exists in `src/`" | `security/project_config.py::read_secret_scan_policy` reads it, with a production caller at `application/proposal_service.py:1148`. Narrowed to the key: nothing reads `providers.review.repositories`, which is what the conclusion needs and what `project_config.py:11` records as deliberate. |
| **C-2** (HIGH, one class) | `threat-model.md` T-7, T-15, T-16 — body and summary rows | three not-shipped controls citing CLOSED issues as their owners | #129, #39 and #198 were all closed `COMPLETED` after a *documentation* fix while the control each named stayed unbuilt. Repointed at live owners, each verified OPEN. |
| **C-3** (HIGH) | `threat-model.md` T-3 correction blockquote | "`theurian.domain.retrieval` has **no importer anywhere in `src/`**" | Five importers, one of them `mcp/results.py` — the module that produces the wire value. What they import is `RaptorPathSegment`, `excerpt`, `EXCERPT_CHARS`, never `SafetyMetadata` or `RetrievalResult`, so the argument holds on the symbols. |
| **C-4** (MEDIUM) | `threat-model.md` T-9 assertion table, row 7 | "seeds a sentinel into all nine sources" | Ten, counted by AST over the `seeds` dict in `_seed_every_external_source`. The sweep is *wider* than the document claimed, so no security decision rested on it — hence MEDIUM. |

**C-2's class, named by root cause:** *an issue filed against a false control
claim was closed by correcting the wording, and the entry kept naming it as the
owner of the control that was never built.* Owners after the repoint, each state
verified with `gh issue view`:

| Control | Was | Now |
| :-- | :-- | :-- |
| T-7: scheme allowlist, private-network rejection, repository allowlist | #129 (CLOSED 2026-08-22) | **#368** (OPEN) — carries all three as acceptance criteria on the fetch path it builds |
| T-16: install-time artifact verification | #39 (CLOSED 2026-08-07) | **#80** (OPEN) — diagnoses this exact split and records that a successor issue is still owed |
| T-15: ingest-time and index-time scanning | #198 (CLOSED 2026-08-24) | **#329** (OPEN) — already named in the threat-model body at T-15; only the summary row was stale |

Two `src` docstrings carry the same class and are corrected in the same PR, both
prose-only (AST-identical once docstrings are stripped):

- `infrastructure/sqlite/index_purge.py::_restamp` — "Nothing in `src/` reads
  `index_metadata.index_build_id` back today" is false;
  `SqliteIndexStore.add_nodes` reads it back to stamp each summary node, and
  `index_store.py`'s own docstring says so. Two comments in one package
  contradicted each other. Narrowed to what the paragraph needs: nothing
  *serves* that column, because `mcp/search.py` publishes `indexBuildId` from
  the pointer.
- `application/project_service.py::verify_state_provenance` — its docstring
  claimed two call sites; it has one, `mcp/tools.py`'s `_resolve`.
  `theurian index build` is gated, but by
  `cli/index_commands.py::_require_buildable_state` calling
  `BuildProvenance.has_state` directly, because it must `_fail` with an exit code
  rather than raise. The control is real; the function named for half of it was
  not the one holding that half.

## M-1 — YAML alias expansion cost in `projection._walk`

Owed by #199's body: `MAX_DEPTH` bounds depth, and breadth via YAML aliases was
recorded as unmeasured, with the guess that `_StrictLoader`'s node cache bounds
it.

**Measured answer: the loader's node cache does not bound the walk.** `_walk`
carries no memo — unlike `_external_refs`'s `descended` — so a node reached by
*n* paths is entered *n* times. What bounds it is `MAX_PROJECTION_CHARS`, charged
in `_Spend.emit` *during* the walk; not `MAX_PROJECTION_NODES`, and not the
parse.

Doubling-alias bomb, `load_yaml` → `project_checked`:

```
levels   bytes parsed objs   2^N expand  walk visits   wall s  peakMB  outcome
    10     263          24        2,047        6,131    0.021    0.31  PROJECTED
    14     375          32       32,767       98,287    0.369    5.70  PROJECTED
    18     487          40      524,287      120,491    0.458    5.01  REFUSED (projected text size)
    22     599          48    8,388,607      120,491    0.451    5.01  REFUSED (projected text size)
    30     823          64 2,147,483,647     120,491    0.456    5.01  REFUSED (projected text size)
    60    1663         124 2.3e18            120,491    0.453    5.01  REFUSED (projected text size)
```

Flat from level 18 upward, however large the nominal expansion. At 22 levels the
parsed graph holds **48** objects — the loader's cache — while the walk enters
**120,491** — the absent memo.

Worst clone-reachable shape: aliased non-empty containers, which cost a visit and
emit nothing, so they maximise visits per emitted character.

```
chain d=23 aliased x1000        11,309 B  visits=  24,301  0.086 s  peak 0.16 MB  PROJECTED
chain d=23 aliased x10000      119,309 B  visits= 240,301  0.910 s  peak 1.59 MB  PROJECTED
chain d=23 aliased x50000      639,309 B  visits= 903,541  3.094 s  peak 3.92 MB  REFUSED (projected text size)
chain d=23 aliased x100000   1,289,309 B  visits= 903,541  3.137 s  peak 3.92 MB  REFUSED (projected text size)
```

Conclusions, each carrying its number:

1. **#232's control holds.** Pre-fix on record: 19.76 s / 2.8 GB from 405 bytes.
   Post-fix worst alias shape measured here: **3.14 s / 3.92 MB**; doubling bomb
   **0.45–0.51 s / 5.01 MB** flat.
2. **The binding budget on the alias path is `MAX_PROJECTION_CHARS`**, not
   `MAX_PROJECTION_NODES`. `projection.py`'s `MAX_PROJECTION_NODES` docstring
   records the node ceiling being reached at 0.48 s by a 422,040-byte document.
   Both figures are true and they are different shapes — that one is refused on
   *nodes*, this one on *characters* — and T-6 did not say which budget answers
   an alias bomb. It does now.
3. **Residual recorded, MEDIUM:** the bounds admit ~3.1 s of GIL-holding
   pure-Python work on the ingestion path from one 1.29 MB clone-reachable
   document, about 6.5× the 0.48 s the code records for the node-ceiling case.
   Availability, not disclosure. Ingestion-side, so #26 (query-side) does not own
   it, and T-6 already records the decision that no ingestion-side timeout is
   filed. **The gap closed here is the figure, not the decision.**

## Handoff — unit C, the served corpus twins

Five faces of these classes live in served corpus items under
`.theurian/knowledge/`, which are POP-3's population and are re-seeded rather
than edited in place:

| Face | Class | Owner |
| :-- | :-- | :-- |
| `ai-writes-produce-proposals` — the falsified proposal-age claim | stale served claim | [#416](https://github.com/theurian/theurian/issues/416) |
| `single-writer-synchronous-in-m1` — the promised `doctor` NFS warning no step implements | claim with no implementing code | [#417](https://github.com/theurian/theurian/issues/417) |
| `raptor-forest…:750` — "`raptorPath` … emitted by nothing" | absence claim about `src/` | unit C |
| `raptor-forest…:1072` — "Nothing in `src/` reads `.theurian/config.yaml`" | C-1's class | unit C |
| `a-purge-is-a-build…:187` — "Nothing in `src/` reads either back today" | the `index_build_id` class | unit C |

`raptor-forest…:750` is false in both halves: `mcp/results.py:105` emits
`raptorPath`, and `schemas/knowledge/retrieval-result.schema.json` declares it.

## Population counts, with the command that produced each

Run from the repository root against `06de58a`. The three scripts live in the
session scratchpad, not the repository; each is a few dozen lines and is
reproduced by the key definitions above.

| Count | Command | Output |
| :-- | :-- | :-- |
| seeds population (C-4) | `199a_count_seeds.py …/test_setup_report_withholding.py` | `seeds dict literal: 10 sources`; `_OBSERVED_SEEDS … 6` |
| 1e members | `199a_key1e.py` | `POP-1e MEMBERS: 21 ( 1e-i 16 + 1e-ii 5 )`, `continuations … 7` |
| container census | `199a_completeness.py` | `bold-labelled block openers : 276`, `inside a key : 88`, `outside every key : 188`, `candidates: 15` |
| verb sweep | `199a_completeness.py` | `total hits : 134`, `inside a keyed member: 32`, `OUTSIDE, to triage : 102` |
| verb-sweep triage | `199a_triage.py` | `v3 6 · diagram_or_testname 5 · narrative 91` (91 + 5 + 6 = 102) |
| C-1 reader | `git grep -n read_secret_scan_policy -- packages/theurian-core/src/` | 4 lines, incl. `proposal_service.py:1148` |
| C-3 importers | `git grep -n "from theurian.domain.retrieval import" -- packages/theurian-core/src/` | 5 lines, incl. `mcp/results.py:29` |
| C-3 narrower premise | `git grep -n "SafetyMetadata\|RetrievalResult" -- packages/theurian-core/src/` | 3 lines, all in `domain/retrieval.py` |
| owner states | `gh issue view N --json state` | #368 #80 #329 OPEN · #129 #39 #198 CLOSED |
